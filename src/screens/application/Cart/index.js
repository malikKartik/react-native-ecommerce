import React from 'react';
import {ScrollView, Text} from 'react-native';

const Cart = () => {
  return (
    <ScrollView>
      <Text>This is cart page!!</Text>
    </ScrollView>
  );
};

export default Cart;
